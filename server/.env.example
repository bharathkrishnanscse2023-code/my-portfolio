# ---- Server ----
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# ---- SMTP / email (Nodemailer) ----
# Gmail: host=smtp.gmail.com port=465 secure=true, SMTP_PASS = App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Where contact messages are delivered
OWNER_EMAIL=bharathkrishnans.cse2023@citchennai.net

# Rate limiting
RATE_WINDOW_MIN=15
RATE_MAX=20
